version: "3.8"

services:
  app:
    image: superklokdotcom:1.11.3
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production