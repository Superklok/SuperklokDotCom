version: "3.8"

services:
  app:
    image: superklok/superklokdotcom:1.11.4
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production